# Самопредложение улучшений плагина

Концепция работы агента-наблюдателя, который замечает шаблоны ручных вмешательств пользователя и периодически предлагает улучшения плагина. Соответствует направлению 5 ROADMAP carono-ai и принципу MISSION «человек направляет, агент исполняет»: система фиксирует сигналы и предлагает, не решает за пользователя.

Документ собирается из четырёх независимых разделов:

- `## Источник` — какой журнал служит сырьём, какие в нём поля, где проходит граница «трение vs направление». **Готов** (см. ниже, задача WF-36).
- `## Анализатор` — кто читает журнал, когда запускается, как извлекает повторяющиеся проблемы. **Готов** (см. ниже, задача WF-37).
- `## Формат вывода` — как агент-наблюдатель формулирует предложение пользователю, что включает, как пользователь принимает или отклоняет. **Готов** (см. ниже, задача WF-38).
- `## Проверка концепции на MISSION` — итоговая сборка и сверка с принципами и анти-целями миссии. **Готов** (см. ниже, задача WF-39).

---

## Источник

### Что считается «трением»

«Трение» — повторяющиеся ручные правки пользователя в работе агентов: одно и то же вмешательство наступает на одни и те же грабли в разных задачах или в разных запусках. Одноразовая правка трением не считается — это шум.

Граница «трение vs направление» проходит по факту попадания в журнал, а не задним числом:

- **Трение** — пользователь правит работу агента, потому что агент сделал не так. Это ошибка агента или дефект инструкции, по которой агент работает. Только такие факты попадают в журнал ручных вмешательств.
- **Направление** — пользователь меняет постановку задачи или формулирует новое требование. Не правка работы, а новый ввод. В журнал не пишется.

Решение «трение это или направление» принимает агент **в момент записи**, по правилам, уже зафиксированным в `workflow/{bot-name}/MANUAL_INTERVENTIONS.md` (раздел «Что считается вмешательством»). По построению: всё, что попало в журнал — потенциальное трение. Анализатор различает «потенциальное» от «подтверждённого» по повторяемости.

### Какой журнал — сырьё

Источник один — журнал ручных вмешательств `workflow/{bot-name}/manual-interventions.jsonl`. Контракт формата зафиксирован в шаблоне `src/templates/MANUAL_INTERVENTIONS.md` (задача WF-32) и копируется в `workflow/{bot-name}/MANUAL_INTERVENTIONS.md` при настройке бота скиллом `configure`.

Параллельного журнала «трения» не существует — это сознательное решение, чтобы:

- не плодить два потока с разной семантикой и риском рассинхронизации;
- удержать стабильный контракт записи (анализатор и пороги можно тюнить, не трогая агентов);
- сохранить принцип MISSION «контракт важнее реализации».

### Поля записи и опциональный признак трения

Обязательные поля записи (`timestamp`, `agent`, `task`, `step`, `what`) описаны в шаблоне `MANUAL_INTERVENTIONS.md`. Они одинаковые для любого вмешательства — анализатор не зависит от того, посчитал ли агент конкретный факт повторяющимся.

Поверх обязательных полей шаблон допускает два **опциональных**:

| Поле | Когда выставляется |
|---|---|
| `friction_signal: true` | Агент в момент записи субъективно уверен, что наступает на повтор (вспомнил предыдущую похожую правку, увидел тот же паттерн). По умолчанию отсутствует. |
| `severity: "minor" \| "major"` | Только если выставлен `friction_signal`. `minor` — мелкая правка (опечатка, формулировка); `major` — переделка логики или возврат к шагу. |

Это подсказка анализатору, не приговор. Окончательное решение «это трение или нет» анализатор принимает по объективным порогам (см. ниже) — наличие или отсутствие `friction_signal` на отдельной записи ничего не предрешает.

### Кто и когда определяет «это трение»

Разделение ролей:

- **Агент пишет факты.** В момент сессии, если выполнены условия из шаблона `MANUAL_INTERVENTIONS.md` («что считается вмешательством»). Сам по себе не считает повторяемость и не читает историю журнала — пишет один факт за одну идентифицированную правку. Может опционально выставить `friction_signal` если уверен.
- **Анализатор читает поток.** Это отдельный процесс (тема WF-37). Он применяет порог повторяемости поверх сырых записей и решает, какие группы записей образуют «трение» — то есть кандидата на улучшение плагина.

Такое разделение даёт:

- **стабильный контракт записи** — поля и условия записи не зависят от того, как именно анализатор считает пороги;
- **возможность тюнить пороги** под проект без правки агентов;
- **минимальную нагрузку на агента** — он не обязан читать историю журнала на каждой записи.

### Пороги повторяемости (значения по умолчанию)

Анализатор применяет следующие пороги по умолчанию (точная реализация — задача WF-37):

| Параметр | Значение по умолчанию | Что означает |
|---|---|---|
| `repeat_count` | `2` | Сколько раз должна повториться похожая запись, чтобы считаться трением. 2 повтора уже повод предлагать улучшение. |
| `window_tasks` | `10` | По скольким последним задачам считается повторяемость. Записи старше окна не учитываются. |

Пороги — **параметры анализатора**, а не часть формата записи. Каждый бот может переопределить их под себя в конфигурации (конкретное место хранения и формат описывает WF-37). При отсутствии переопределения используются значения выше.

«Похожая запись» — это группа записей по совпадению `agent + step + смысловой кластер what`. Алгоритм кластеризации `what` — деталь анализатора (WF-37), не источника.

### Чего источник намеренно не делает

- **Не классифицирует трение по типам** (формат сообщения, неверная команда, потерянный шаг и т.п.). Классификация — задача анализатора и формата вывода.
- **Не хранит severity для всего журнала.** Только опционально для записей с `friction_signal=true`. Принцип «лишние данные не пишем» из WF-32 сохраняется для общего потока.
- **Не отделяет «свежее трение» от «старого».** Окно наблюдения применяет анализатор; журнал хранит всю историю.
- **Не решает, что предлагать пользователю.** Это задача формата вывода (WF-38).

---

## Анализатор

### Кто читает журнал

Анализ — режим существующего скилла `analyst`. Отдельный скилл/агент не заводится: `analyst` уже умеет работать с трекером, MISSION, ROADMAP и историей прогонов, журнал ручных вмешательств для него — ещё один источник сырья. Соответствует принципу MISSION «цикл, а не инструмент»: новая способность встраивается в существующего участника, а не плодит отдельную утилиту.

Активируется отдельным режимом запуска (рабочее имя — `--mode improver`); поведение по умолчанию — `top-down` / `drill-down`, как сейчас. В режиме improver `analyst` не предлагает новые задачи из ROADMAP — только разбирает накопленный журнал и сообщает результат пользователю.

### Когда запускается

Триггер — ручной, через `check.sh` с дополнительным параметром (точное имя — деталь скилла `checker-setup`, например `./check.sh --improver` или `./check.sh --run analyst --mode improver`). Расписание и пороги «N записей в журнале» в этой итерации **не закладываются**: пока не накопится опыт реальных запусков, автоматический триггер преждевременен. Условия пересмотра — после нескольких ручных прогонов станет видно, имеет ли смысл периодичность.

### Входы

`analyst` в режиме improver читает:

| Источник | Что берётся | Зачем |
|---|---|---|
| `workflow/{bot-name}/manual-interventions.jsonl` | весь поток записей | сырьё для анализа повторяемости |
| `workflow/{bot-name}/MISSION.md` (или корневой) | принципы и анти-цели | фильтр: предлагаемое улучшение не должно противоречить миссии |
| `workflow/{bot-name}/ROADMAP.md` | текущая фаза, направления | контекст: к какому направлению приклеивается находка |
| `workflow/{bot-name}/analyst/history.md` | прошлые прогоны (включая прогоны improver) | антидубль: не предлагать то, что уже отклонено или принято в прошлый раз |

Дополнительно при необходимости — `get-task` / `list-comments` по задачам, упомянутым в записях журнала, чтобы уточнить контекст (например прочитать комментарий MR, на который ссылается запись).

### Что анализатор делает

Поведение в одном прогоне:

1. **Загружает журнал.** Читает `manual-interventions.jsonl` целиком, парсит построчно. Битые строки пропускает с предупреждением, но не падает.
2. **Фильтрует по окну наблюдения.** По умолчанию — последние `window_tasks = 10` уникальных задач (`task` в записи). Параметр окна берётся из конфигурации бота, при её отсутствии — значение из раздела «Источник».
3. **Группирует записи.** Ключ группировки — совпадение `agent + step + смысловой кластер what`. «Смысловой кластер» — это решение конкретного запуска (не алгоритм с фиксированным порогом схожести): `analyst` читает `what` целиком и собирает группы вручную, опираясь на смысл, а не на строковое равенство. Опциональный `friction_signal: true` повышает вес записи в группе, но не создаёт группы сам по себе.
4. **Применяет порог повторяемости.** Группы с `count >= repeat_count` (по умолчанию `2`) — кандидаты в «подтверждённое трение». Группы с `count < repeat_count` — пока шум, не выходят из анализатора.
5. **Сверяет с историей.** По каждой группе-кандидату проверяет `analyst/history.md`: не было ли предложение по этому же кластеру уже создано/отклонено. Если было создано — упоминает существующую задачу. Если отклонено — молчит (правило аналитика: не предлагать повторно отклонённое).
6. **Сверяет с миссией.** Если предлагаемое улучшение явно противоречит принципу или анти-цели MISSION — поднимает это отдельно (по аналогии с шагом 9.5 текущего скилла), не подмешивая в общий список.
7. **Сообщает результат пользователю в текущей сессии.** Формат — задача WF-38; здесь анализатор только готовит структурированное содержимое: список подтверждённых трений с привязкой к записям журнала и направлению ROADMAP.

### Выходы

В этой итерации — **только информирование пользователя в сессии запуска**. Анализатор:

- **не создаёт задач в трекере автоматически** — даже если кандидат прошёл все фильтры. Решение «оформлять ли это задачей» принимает пользователь по итогам сессии, обычным запуском `analyst` без режима improver или ручным созданием.
- **не пишет PR с патчем** — патчи к самому плагину остаются ручной работой; анализатор только показывает место, где плагин трётся о реальность.
- **не комментирует задачи в трекере** — это вмешательство в чужие задачи без явного запроса.

Информирование оставлено как минимальный жизнеспособный выход: пользователь увидит результат, оценит полезность, дальше решим — добавлять автоматизацию (создание задач, PR, ROADMAP-обновления) или оставить как есть.

### Запись прогона в history

Каждый запуск improver-режима добавляет запись в `workflow/{bot-name}/analyst/history.md` по аналогии с обычным прогоном `analyst`, чтобы:

- следующий прогон знал, какие группы трения уже показывались пользователю (антидубль);
- была видна история «что плагин научился ловить» — даже если в задачи это пока не оформлено.

Минимальный набор полей записи прогона improver:

- дата (абсолютная, `YYYY-MM-DD`);
- режим: `improver`;
- размер окна и порог, фактически применённые в прогоне;
- количество записей в журнале, подпавших под окно;
- список подтверждённых групп трения (короткое имя кластера + `count` + список `task`);
- решение пользователя по каждой группе: показано / отложено / отклонено / превратилось в задачу `#…`.

### Чего анализатор намеренно не делает

- **Не редактирует журнал.** `manual-interventions.jsonl` — append-only по построению WF-32; анализатор только читает.
- **Не правит MISSION/ROADMAP.** Если найденное трение указывает на несовпадение плагина с миссией — это повод для диалога, не для автоправки.
- **Не запускает других агентов.** Не вызывает `worker` / `discussion` / `reviewer` сам — только показывает находки.
- **Не классифицирует трение по типам в фиксированной таксономии** (формат, команда, шаг и т.п.). Кластеризацию делает по смыслу `what`, без принуждения к фиксированному списку категорий — это задача формата вывода (WF-38), если вообще понадобится.
- **Не привязан к конкретному трекеру.** Все обращения к задачам — через `./workflow/{bot-name}/scripts/tracker`, как и у обычного `analyst`.

---

## Формат вывода

### Способ доставки

Единственный канал — **сообщение в текущей сессии запуска**, в которой пользователь сам запустил `analyst --mode improver`. Агент только формулирует предложение текстом, ничего не меняет в репозитории и в трекере без явной команды пользователя.

Перечень того, что наблюдатель **намеренно не делает** в качестве доставки:

- **Не создаёт задачу в трекере автоматически** — даже если кандидат прошёл все фильтры. Создание задачи происходит только после явного `+` от пользователя (см. ниже).
- **Не открывает PR с патчем плагина** — патчи к ядру плагина остаются ручной работой человека или отдельным запуском `worker` уже на оформленной задаче.
- **Не комментирует чужие задачи в трекере** — наблюдатель не вмешивается в переписку по существующим тикетам, на которые ссылается журнал. Ссылки на эти задачи он только показывает пользователю как контекст.
- **Не правит MISSION/ROADMAP/документы напрямую.** Если находка указывает на расхождение с миссией — это повод для отдельного разговора и отдельной задачи, не для автоправки.

Решение «оформлять ли это задачей и какой именно» принимает пользователь по итогам сессии. Если он принял предложение — наблюдатель тут же в этой сессии создаёт задачу через `./workflow/{bot-name}/scripts/tracker create-task`, по аналогии с обычным прогоном `analyst`. Содержимое задачи — формулировка из карточки предложения (см. поля ниже), стартовая колонка — обычная для нового тикета (определяется в `WORKFLOW.md` бота).

Этот ограниченный канал доставки — прямое следствие анти-цели MISSION «Заменить человека в принятии решений — агенты исполняют, человек направляет». Наблюдатель видит сигнал, формулирует предложение, кладёт его перед человеком — и останавливается.

### Структура одного предложения

В прогоне наблюдатель показывает **список карточек предложений** — по одной на каждую группу подтверждённого трения, найденную анализатором (см. раздел «Анализатор»). Каждая карточка имеет фиксированную структуру:

| Поле | Что в нём |
|---|---|
| **Заголовок** | Короткая формулировка проблемы в одну строку. Пример: «`worker` многократно правит формат коммита, одна и та же претензия пользователя в разных задачах». |
| **Что обнаружил** | 1–3 фразы про суть повтора: какой агент, на каком шаге, в каком виде проявляется ручная правка. Без воспроизведения полных текстов записей — это контекст, а не отчёт. |
| **Статистика повторений** | `count` (сколько записей в группе), окно (`N последних задач`), список задач с короткими номерами (`WF-21, WF-24, WF-29`). При необходимости — даты первой и последней записи в группе. |
| **Конкретное предложение** | Точечное изменение плагина, а не абстрактная идея. Формулировка по схеме «в `<артефакт>` добавить/изменить `<что>`, чтобы агент перестал делать `<повтор>`». Артефакт — это конкретный файл из `src/` (`src/agents/<…>.md`, `src/rules/<…>.md`, `src/templates/<…>.md`, `src/skills/<…>/SKILL.md`). Если предложение не сводится к одному файлу — наблюдатель честно пишет «не формируется как точечная правка» и оставляет поле как открытый вопрос для обсуждения, не выдумывает. |
| **Риски и побочные эффекты** | Что может сломаться, если предложение принять без анализа: какие сценарии работали раньше, кому и как это может усложнить жизнь. Если рисков не видно — пишется явно «рисков на текущий взгляд не вижу», а не пропускается. |
| **Ссылки на сырьё** | Перечень `task` из записей журнала, которые сложились в эту группу. Это «улики» предложения — пользователь может в любой момент открыть их и проверить. |
| **Решение пользователя** | По умолчанию пусто, ждёт `+` / `-` / `?` от пользователя (см. ниже). |

Минимальный набор полей карточки — **все**, кроме «Решения пользователя» (оно появляется по итогам диалога). Если какое-то из обязательных полей наблюдатель не может заполнить — это сигнал, что находка ещё не дозрела до предложения, и её нужно оставить в анализаторе на следующий прогон, а не выдавать «полупредложение» с пробелами.

### Принятие, отклонение и откладывание

Пользователь реагирует на каждую карточку отдельно, по той же системе команд, что и в обычном `analyst`:

| Команда | Что означает | Что делает наблюдатель |
|---|---|---|
| `+` | Принять — оформить как задачу в трекере для последующего обсуждения и работы. | Создаёт задачу через `scripts/tracker create-task`, заголовок и описание берёт из карточки (заголовок → `--title`, остальные поля кроме «Решения» → `--description-file`). В описании ставит ссылки на исходные записи. В стартовую колонку для новых задач (по `WORKFLOW.md`). После создания записывает в карточку номер созданной задачи. |
| `-` | Отклонить — это не проблема плагина, либо не стоит правки. | Никаких действий в трекере. Запись отклонения уходит в `analyst/history.md` как «отклонено», чтобы следующий прогон не предложил это же повторно. |
| `?` | Сомневаюсь — нужен дополнительный разговор. | Никаких действий в трекере. Запись уходит в `analyst/history.md` как «отложено» с пометкой команды пользователя. На следующем прогоне наблюдатель имеет право предложить ту же группу заново — отклонения здесь нет. |

Любое другое сообщение пользователя в ответ на карточку наблюдатель трактует как обычный диалог: уточняет, отвечает на вопросы, при необходимости переформулирует — но **не создаёт задачу**, пока не получил `+`.

Принципиально, что асинхронного «согласия по умолчанию» нет: если пользователь промолчал и закрыл сессию — значит, ничего не принято. Молчание не равно согласию. Это сознательный «зазор», прямо вытекающий из анти-цели MISSION.

### Принудительный «зазор» между предложением и действием

Запретами наблюдателя на этот «зазор» работают два правила:

1. **Любое изменение кода/плагина — только через обычный цикл задачи** (`worker` → MR → `reviewer`), и только если задача оформлена явным `+` пользователя. Наблюдатель не имеет права инициировать правки сам, даже когда «всё очевидно».
2. **Наблюдатель не запускает других агентов из своей сессии.** Не вызывает `worker`, `discussion`, `reviewer`. Создание задачи через CLI трекера — единственное действие, на которое он способен после `+`, и только потому, что это симметрично обычному прогону `analyst`.

Эти ограничения дублируют ограничения анализатора (см. раздел «Анализатор», подраздел «Чего анализатор намеренно не делает») — они здесь повторены сознательно, чтобы формат вывода нельзя было прочитать в отрыве: правила доставки и правила анализа смыкаются в одно и то же ограничение.

### Запись в `analyst/history.md`

По итогам прогона наблюдатель добавляет одну запись в журнал прогонов аналитика — в формате, согласованном с обычным прогоном (см. раздел «Анализатор», подраздел «Запись прогона в history»). На этот раздел поверх стандартных полей добавляется по каждой карточке:

- решение пользователя (`+` / `-` / `?`);
- если `+` — номер созданной задачи;
- если `-` — короткая формулировка причины отклонения, если пользователь её дал. Если не дал — поле остаётся пустым, наблюдатель не выдумывает.

Эта запись — основа антидубля: на следующем прогоне анализатор сверяется с ней и не предлагает повторно отклонённое (`-`), но имеет право снова предложить отложенное (`?`).

---

## Проверка концепции на MISSION

Этот раздел сводит три предыдущих в единое целое и прогоняет получившуюся концепцию через вопросы-маркеры MISSION (раздел «Для агентов разработки») и через анти-цели. Цель проверки — убедиться, что наблюдатель не нарушает базовые принципы плагина, и зафиксировать места, где концепция держится только за счёт сознательных ограничений (зазоров).

### Q1. Это работает в любом проекте или только в конкретном?

**Ответ: в любом проекте, где боты подключены через стандартную настройку `configure`.**

Концепция опирается только на универсальные опоры плагина:

| Опора | Где живёт | Зависит ли от трекера/стека |
|---|---|---|
| Журнал `manual-interventions.jsonl` | `workflow/{bot-name}/` | Нет. Контракт формата зафиксирован в `src/templates/MANUAL_INTERVENTIONS.md` (WF-32) и копируется через `configure`. |
| Чтение MISSION / ROADMAP / `analyst/history.md` | файлы проекта | Нет. Это плоские markdown-документы. |
| Создание задач из принятой карточки | `./workflow/{bot-name}/scripts/tracker create-task` | Нет. CLI-контракт един для любого трекера (YouGile, GitHub, GitLab, Jira). |
| Опциональное чтение комментариев задач из журнала | `./workflow/{bot-name}/scripts/tracker get-task` / `list-comments` | Нет. Тот же CLI-контракт. |

Хардкодов конкретного трекера, языка реализации или CI в концепции нет. Принцип «универсальность важнее удобства» соблюдён.

**Граничное условие.** Если на проекте боты ещё не пишут `manual-interventions.jsonl` (например, бот настроен старой версией `configure` до WF-32) — анализатору нечего читать. Это не нарушение универсальности: режим improver остаётся опциональным, отсутствие журнала просто даёт пустой прогон с честным сообщением «нет данных».

### Q2. Это звено цикла или изолированный инструмент?

**Ответ: звено.**

Концепция намеренно встраивается в существующий участник цикла:

- **Источник данных** — журнал, который пишут агенты `discussion / worker / reviewer` во время своей обычной работы. Отдельного потока «трения» не заводится (см. раздел «Источник», подраздел «Какой журнал — сырьё»).
- **Кто читает** — режим существующего скилла `analyst`, а не новый агент или новая утилита. Соответствует принципу MISSION «цикл, а не инструмент» и явному выбору «не плодить отдельного участника» (см. раздел «Анализатор», подраздел «Кто читает журнал»).
- **Что делает с находкой** — оформляет в обычную задачу через стандартный `scripts/tracker create-task`, дальше она идёт по тому же циклу `discussion → worker → reviewer`. Никакого параллельного маршрута для «улучшений плагина» не создаётся.

**Зазор честно зафиксирован.** Между «анализатор увидел трение» и «в репозитории появилась правка» стоит обычный цикл: предложение → `+` от пользователя → задача → MR → ревью. Наблюдатель не короткозамыкает цикл и не запускает других агентов из своей сессии (см. раздел «Формат вывода», подраздел «Принудительный "зазор" между предложением и действием»). Это полузвено по построению — и это сознательное соответствие анти-цели «человек направляет», не дефект.

### Q3. Усложнит ли это подключение плагина?

**Ответ: не усложняет.**

Что появляется на стороне нового бота при подключении плагина:

- `MANUAL_INTERVENTIONS.md` уже копируется текущим `configure` (зафиксировано WF-32) — отдельной установки концепция не требует.
- Режим `--mode improver` встроен в существующий скилл `analyst`. Новых файлов в плагине под него заводить не нужно — это поведение, описанное в SKILL.md.
- Триггер запуска — параметр существующего `check.sh`. Пользователь и так запускает `check.sh` — добавляется одна опция, не отдельный шаг.
- Конфигурация порогов (`repeat_count`, `window_tasks`) — опциональная: при отсутствии переопределений работают значения по умолчанию из раздела «Источник».

Барьер «одна команда подключения» не нарушен. Принцип MISSION «минимальный барьер входа» соблюдён.

**Что добавилось бы при нарушении этого принципа** (для контраста, чтобы стало видно, чего избегаем): отдельный демон, отдельный конфиг с правилами кластеризации, отдельная база данных для статистики, новый MCP-сервер. Концепция отказалась от всего этого сознательно (см. раздел «Анализатор», подраздел «Чего анализатор намеренно не делает»).

### Q4. Это фиксируется как контракт или как код-шаблон?

**Ответ: всё, что вынесено наружу — как контракт; алгоритмы оставлены под живое решение `analyst` в момент прогона.**

| Что фиксируем | Где живёт | Тип фиксации |
|---|---|---|
| Поля записи журнала (`timestamp`, `agent`, `task`, `step`, `what`, опц. `friction_signal`, `severity`) | `src/templates/MANUAL_INTERVENTIONS.md` | Контракт формата. Меняется редко, переживает смену реализации агентов. |
| Пороги повторяемости (`repeat_count`, `window_tasks`) | конфигурация бота, значения по умолчанию в разделе «Источник» | Контракт через значения. Параметры, не код. |
| Структура карточки предложения | раздел «Формат вывода» | Контракт документа. Любая реализация `analyst --mode improver` обязана выдавать карточку именно с этими полями. |
| Команды пользователя (`+` / `-` / `?`) и их семантика | раздел «Формат вывода», подраздел «Принятие, отклонение и откладывание» | Контракт интерфейса с пользователем. Симметричен обычному прогону `analyst`. |
| Запись прогона в `analyst/history.md` | раздел «Анализатор», подраздел «Запись прогона в history» | Контракт антидубля. На него опирается следующий прогон. |
| Алгоритм кластеризации `what` | внутри `analyst` в момент прогона | **Намеренно не контракт.** Решение конкретного запуска по смыслу, а не фиксированный алгоритм с порогом схожести. |
| Маршрут создания задачи после `+` | `./workflow/{bot-name}/scripts/tracker create-task` | Контракт CLI трекера. Реализация под конкретный API живёт в скрипте, не в концепции. |

Принцип MISSION «контракт важнее реализации» соблюдён в обе стороны: что должно переживать смену API/реализации — описано как контракт; что должно подстраиваться под живое сырьё (кластеризация смыслов) — оставлено как решение `analyst`, чтобы не превращать живой алгоритм в устаревающий шаблон.

### Сверка с анти-целями

| Анти-цель MISSION | Где в концепции мы её соблюдаем |
|---|---|
| **«Создать агентов под конкретный трекер или стек».** | Концепция не содержит привязки к YouGile/GitHub/Yii2/чему-либо. Все обращения к трекеру — через CLI-контракт. Журнал — markdown/JSONL без привязки к стеку. |
| **«Покрыть все возможные воркфлоу из коробки».** | Концепция работает только если бот пишет журнал и пользователь запускает `analyst --mode improver`. Не пытается ловить трение там, где плагин не подключён. Опциональность зафиксирована: на «пустом» проекте режим даёт пустой прогон, не падает и не пытается «угадать» сигналы. |
| **«Заменить человека в принятии решений — агенты исполняют, человек направляет».** | Главный зазор концепции. Наблюдатель **только сообщает** в сессии. Не создаёт задач без `+`, не открывает PR с патчем, не комментирует чужие задачи в трекере, не запускает других агентов, не правит MISSION/ROADMAP. Молчание пользователя ≠ согласие (раздел «Формат вывода»). |

### Соответствие принципам MISSION (сводно)

| Принцип | Где соблюдён |
|---|---|
| Универсальность важнее удобства | Q1, Q3. Хардкодов трекера/стека нет. |
| Агент — читатель, не исполнитель хардкода | Q4. Кластеризация — живое решение по смыслу `what`, без жёсткого алгоритма. |
| Минимальный барьер входа | Q3. Новых файлов на стороне бота нет, режим встроен в существующий скилл. |
| Цикл, а не инструмент | Q2. Встройка в `analyst`, выход через стандартный `create-task` в обычный цикл. |
| Документы живут с кодом | Концепция сама — документ в `docs/`, история прогонов — `analyst/history.md`, контракт записи — `src/templates/MANUAL_INTERVENTIONS.md`. |
| Контракт важнее реализации | Q4. Все межграницы зафиксированы как контракты, реализации остаются гибкими. |

### Найденные противоречия

**Противоречий с MISSION не обнаружено.** Концепция целиком держится на уже принятых в плагине контрактах (журнал WF-32, CLI трекера, скилл `analyst`) и сознательных «зазорах» (информирование вместо автодействия, явный `+` пользователя, отсутствие саморасшифровки в код). Ни один из четырёх вопросов-маркеров не вернул отрицательного ответа, ни одна анти-цель не нарушена.

Поднимать в WF-35 решение «расширить миссию или скорректировать концепцию» не требуется.

### Что не входит в эту концепцию (зафиксировано явно)

Чтобы будущие задачи на реализацию не уехали за рамки концепции, перечислим явно, чего здесь намеренно нет:

- **Автоматическое создание задач без `+`.** Только после явной команды пользователя. Если в реализации появится «авто-приём по таймеру/порогу» — это нарушение анти-цели MISSION.
- **Автоматический PR с патчем плагина.** Любая правка плагина идёт через обычный цикл задачи (`worker` → MR → `reviewer`).
- **Самозапуск других агентов из сессии improver.** Запрещено.
- **Расписание / автоматический триггер по «N записей в журнале».** В этой итерации не закладывается — запуск только ручной (см. раздел «Анализатор», подраздел «Когда запускается»).
- **Внешняя память агентов / RAG.** За горизонтом текущей фазы (см. ROADMAP, раздел «За горизонтом»).

### Готовность к реализации

Концепция собрана, согласована с MISSION и не требует изменений миссии. Реализация выходит за рамки задачи WF-39 — она будет открыта отдельной задачей после одобрения этого документа пользователем (см. постановку WF-39: «После одобрения концепции пользователем — открыть отдельную задачу на реализацию»).
